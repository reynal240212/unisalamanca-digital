/**
 * Script for Vercel compatibility.
 * Since the project is now organized at the root,
 * this step is no longer required to copy files.
 */

console.log('✅ El proyecto ya está organizado en la raíz. Omitiendo preparación.');
process.exit(0);
